package PVE::Tools;

use strict;
use warnings;
use Exporter qw(import);

our @EXPORT_OK = qw(file_read_firstline file_set_contents run_command);

sub file_read_firstline { return undef; }
sub file_set_contents { return; }
sub run_command { return 0; }

1;
